using UnityEngine;

public class WoodenBox : MonoBehaviour
{
    // Should be pushable by brute,punch,shockwave
    //should bot be pushable by parasite
    //should be grabable by cat
    //should be able to push buttons
}
